# open
my open function

dependencies:
  - mupdf
  - imagemagick display
  - exo-open (also works with xdg)

usage:
  - alias open='~/path/to/open.sh'
  - ~$ open [file|string]
  - finds strings in files
